name: CI

on:
  pull_request:
  push:
    # `develop` entra el 2026-08-26 con la FASE 3. Desde hoy el trabajo aterriza ahi y no en `main`:
    # dejar solo `main` habria hecho que el push que de verdad integra el cambio no ejecutara nada,
    # y que la primera senal llegara en la release. Las PRs ya lo disparan; esto cubre el push de
    # despues del merge.
    branches: [main, develop]

# Least privilege: this workflow only reads the repo.
permissions:
  contents: read

# One run per ref; cancel superseded PR runs, never cancel main.
concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: ${{ github.ref != 'refs/heads/main' }}

jobs:
  validate-configs:
    name: Validate tsconfig presets
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@d23441a48e516b6c34aea4fa41551a30e30af803 # v6.1.0

      - name: Setup Node
        uses: actions/setup-node@249970729cb0ef3589644e2896645e5dc5ba9c38 # v6.5.0
        with:
          node-version-file: .nvmrc

      # Pinned TypeScript matching the package's `typescript >=5.5` peerDep.
      # Installed standalone (the package ships no deps and no lockfile).
      #
      # The `renovate:` comment is load-bearing: this pin lives in a `run:` step, which no
      # Renovate manager parses, so without it the version is invisible to the bot and only
      # moves when a human remembers. It is the same class of blind spot as an unpinned action.
      - name: Install pinned TypeScript
        # renovate: datasource=npm depName=typescript
        run: npm install --no-save --no-package-lock typescript@5.9.3

      - name: Validate configs compile
        run: npm run validate
